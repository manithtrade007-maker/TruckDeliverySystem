.DS_Store
backend/data.backup-*.json
backend/backups/
backend/*.db
backend/*.db-*

# Built frontend — regenerated by Render on deploy (npm run build)
frontend/dist/

# Dependencies — regenerated by npm install (package-lock.json pins versions)
node_modules/
